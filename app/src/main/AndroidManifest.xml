<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplicativo"
        tools:targetApi="31">

        <activity android:name=".EnviadoActivity" android:exported="false" />
        <activity android:name=".PedidosActivity" android:exported="false" />
        <activity android:name=".QuiztresActivity" android:exported="false" />
        <activity android:name=".QuizdoisActivity" android:exported="false" />
        <activity android:name=".JoaoActivity" android:exported="false" />
        <activity android:name=".MateusActivity" android:exported="false" />
        <activity android:name=".PedroActivity" android:exported="false" />
        <activity android:name=".TomeActivity" android:exported="false" />
        <activity android:name=".QuizActivity" android:exported="false" />

        <!-- Tela principal -->
        <activity android:name=".Principal" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
